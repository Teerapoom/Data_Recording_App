// model ไว้นิยามก้อนข้อมูล กี่ส่วน

  class Transactions {
    String title;
    double amount;
    DateTime data;

    Transactions({
      required this.title,
      required this.amount,
      required this.data
      });
  }
